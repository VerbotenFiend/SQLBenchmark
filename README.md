# LAB
in questa versione ollama non va risponde, ma non è un problema perche comunque va staccato e attaccato al text-to-sql. La SQL request va e restituisce la risposta nel formato corretto, ma non viene visualizzata correttamente: si ha sia una versione corretta ma brutta, che la versione sbagliata. Per non creare problemi, questa versione sarà tenuta di backup mentre aggiusto la resa grafica.
